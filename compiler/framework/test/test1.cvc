// SPATIES  + NEWLINES + MULTI LINE COMMENTS FIXEN

// GlobalDec
extern int a;
extern int [a, b] a; // PRINT NOG EVEN FIXEN

// Functiondec
extern int foo (int p); // PRINT NOG EVEN FIXEN

// GlobalDef
export int sum = 1 + 2;
int sum = 4 + 3;
int a;

// Functions
int foo () {}
int bar (int p) {} // PARAM PRINT
int foobar (int [a, b] p) {} // PARAM PRINT

export void main (int a, int b) {
// Vardeclarations
    int a = a + b;
    int b = 3 + 2;
    int c; // PRINT NOG EVEN FIXEN

    // Localfunctions
    int hallo (){} // PRINT NOG EVEN FIXEN

// Statements
    a = 43.1234567 - 2;
    b = true * 7;
    c = foo(1);
    d = foo(1 + 2);
    e = ((!a + c) <= b) <= 3;
    f(a);
    g = a[1, 2]; // PRINT NOG EVEN FIXEN
    h = array[a, b]; // PRINT NOG EVEN FIXEN

    if (a == b) 
    {
    //    j = foo(1 + 2); FIX BLOCK
    //    k = foo(a + b); FIX BLOCK
    }

     if (q) 
        l = foo(a + 3);
     else
        m = foo(a + 4);

    // while (a < b) { n = test(1); o = test(2); } FIX BLOCK
    while (c) p = test(3);

    return 3;
    return;

    foo();
    foo(bar);
    foo(foo, bar);
}