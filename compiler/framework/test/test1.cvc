// GlobalDec
extern int a;

// Functiondec
extern int foo (int p); // PRINT NOG EVEN FIXEN

// GlobalDef
export int sum = 1 + 2;
int sum = 4 + 3;
int a;

// Functions
int foo () {}
int bar (int p) {}

export void main (int a, int b) {
// Vardeclarations
    int a = a + b;
    int b = 3 + 2;
    int c; // PRINT NOG EVEN FIXEN

// Localfunctions
    int hallo (){} // PRINT NOG EVEN FIXEN

// Statements
    a = 43.1234567 - 2;
    b = true * 7;
    c = foo(1);
    d = foo(1 + 2);
    e = ((!a + c) <= b) <= 3;
    f(a);

    if (a == b) 
    {
        j = foo(1 + 2);
        k = foo(a + b);
    }

    if (q) 
        l = foo(a + 3);
    else
        m = foo(a + 4);

    while (a < b) { n = test(1); o = test(2); }
    while (c) p = test(3);

    for (int i = 0, i - 3, a + b) {
        n = foo(bar);
    }

    for (int i = 0, i - 3) {
        n = foo(bar);
    }

    return 3;
    return;

    foo();
    foo(bar);
    foo(foo, bar);
}