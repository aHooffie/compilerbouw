/* Typechecking. */

// globals
int a = 0;
int b = 2;

float c = 0.5;
float d = 4.5;

bool e = true;
bool f = false;

export void main ()
{
//	a = 1 + 3 * 2; // INT + INT + INT WERKT NIET!
	b = 3 - a; // INT + varINT
	c = 3.4 / c; // FLOAT + varFLOAT
	a = a + b; // varINT + varINT
	f = c >= d; // varFLOAT + varFLOAT
	f = !e;

// MAG NIET:
//	a = a < d; // varINT + varFLOAT
//	c = c <= e; // varFLOAT + varBOOL
//	a = a % c; // varINT + varFLOAT
//	f = e + true; // varBOOL + BOOL
// 	f = !d;

}