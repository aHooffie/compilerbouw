// GlobalDec
extern int a;
extern int [a, b] a;

// Functiondec
extern int foo (int p);

// GlobalDef
export int sum = 1 + 2;
int sum = 4 + 3;
int a;

// Functions
int foo () {}
int bar (int p) {} 
int foobar (int [a, b] p) {}

export void main (int a, int b) {
// Vardeclarations
    int a = a + b;
    int b = 3 + 2;
    int c;
    int [1, 2] d;
    // int [1, 2] g = a;
    // int [1, 2] e = [a];
    // int [1, 2] f = [a, b];

// Localfunctions - PRINT NIET!!
   int hallo (){} 
   int hoi (int a, int b) {}

// Statements
    a = 43.1234567 - 2;
    b = true * 7;
    c = foo(1);
    d = foo(1 + 2);
    e = ((!a + c) <= b) <= 3;
    f(a);

    // Arrays
    g = a[1, 2];
    h = array[a, b];
    a[a, b] = 4;

    if (a == b) 
    {
        j = foo(1 + 2); 
        k = foo(a + b); 
        if (a == c) 
        {
            foo(bar(1));
        }
    }

     if (q) 
        l = foo(a + 3);
     else
        m = foo(a + 4);

    while (a < b)
    {
         n = test(1);
         o = test(2);
    }

    while (c)
        p = test(3);


    /* 
        testen
        van comments
     */

    return 3;
    return;

    foo();
    foo(bar);
    foo(foo, bar);
}