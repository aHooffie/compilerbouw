export void main (int m, int n) {
// Vardeclarations
    int o = 3 * 6;
    int r = 3 + 2;
    int s = r + o;
    int s = o;
    m = o; 
}

export int foo(int p) {
    int a;
    int b;
    int b = a;
    b = a;
    
    p = main(a, b); // mag
    p = main(c, d); // mag niet (parameters)
    main(); // mag ook niet (parameters)

    m = r + s; // mag ook niet
}